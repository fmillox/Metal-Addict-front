const initialState = {
  isDisplayed: false,
};

function picturesReducer(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}

export default picturesReducer;
