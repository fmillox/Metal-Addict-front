import React from 'react';

import './modifyProfile.scss';

const ModifyProfile = () => (
  <div className="modifyProfile">
    <div>Ajouter / modifier une photo de profil</div>
    <div>Ajouter / Modifier le pseudo</div>
    <div>Ajouter / Modifier une introduction</div>
  </div>
);

export default ModifyProfile;
